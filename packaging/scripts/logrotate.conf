/var/log/kyoto.log {
        daily
        missingok
        rotate 5
        compress
        delaycompress
        notifempty
        create 644 kyoto kyoto
        sharedscripts
        postrotate
            test -f /var/run/kyoto.pid && kill -HUP `cat /var/run/kyoto.pid`
        endscript
}
